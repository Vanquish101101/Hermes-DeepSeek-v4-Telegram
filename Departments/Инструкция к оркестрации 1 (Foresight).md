# Инструкция к оркестрации 1 (Foresight)

> Подход «Foresight»: **лёгкая оркестрация** — Hermes решает задачи сам, опираясь на
> правильный выбор MCP-инструментов (поиск/исследование/скрап/память), без тяжёлого
> департамента субагентов. Альтернатива — «Инструкция к оркестрации 2 (Мой вариант)».

Версия: **1 (Foresight)**. Дата старта: 21.06.2026.
Связано: `SOUL.md` Hermes (правила), `Departments/Инструкция к оркестрации 2 (Мой вариант).md` (альтернативный алгоритм, на паузе).

---

## Идея подхода
Вместо департамента из 31 субагента — Hermes как один сильный исполнитель, который для каждой
подзадачи берёт оптимальный инструмент по типу и стоимости (приоритет MCP). Дёшево, быстро,
прозрачно. Разведка и факты — через Perplexity (ответ из живого веба с источниками).

---

## Проделанные этапы

### Этап 0 — Подключение и подтверждение Perplexity MCP (21.06.2026)
Подтверждено на всех уровнях:
- **Конфиг:** `config.yaml` → `perplexity` = `npx -y @perplexity-ai/mcp-server` (v0.9.0), `enabled: true`.
- **Hermes видит MCP:** `hermes mcp list` → `perplexity … ✓ enabled`.
- **Ключ (эмпирически):** прямой запрос к `api.perplexity.ai/chat/completions` → **HTTP 200**, ответ + живые цитаты (`last_updated` 21.06.2026), стоимость ~$0.005/запрос. Ключ `pplx-R1jb…` валиден.

### Этап 1 (пункт «а») — Роутинг инструментов поиска/исследования в SOUL.md
Добавлено **Правило 5** в `C:\Users\Unknown\AppData\Local\hermes\SOUL.md` («Роутинг инструментов поиска и исследования»):

| Задача | Инструмент |
|--------|-----------|
| Свежий факт / ресёрч + нужны источники-пруфы | **Perplexity** `sonar` / `sonar-pro` |
| Глубокое исследование, развёрнутый отчёт | **Perplexity** `sonar-deep-research` |
| Анализ / рассуждение поверх найденного | **Perplexity** `sonar-reasoning-pro` |
| Прочитать конкретный известный URL | Firecrawl / WebFetch |
| Массовый структурированный скрап | Apify |
| Документация библиотек / фреймворков | Context7 |
| Рассуждение над данным контекстом, большой контекст | Gemini / OpenRouter |
| Пульс соцсетей / X | x_search (grok) |

**Ниша Perplexity:** «вопрос → синтезированный ответ из живого веба **с источниками**». Модели по стоимости: `sonar` → `sonar-pro` → `sonar-reasoning-pro` → `sonar-deep-research` (брать минимально достаточную; для `sonar` `max_tokens` ≥ 16).

### Этап 2 (пункт «б») — Perplexity как основной инструмент OSINT/разведки
В шаге разведки (когда он применяется) основной инструмент — Perplexity MCP (`sonar`/`sonar-pro` для фактов с источниками, `sonar-deep-research` для отчёта по нише/конкурентам); дополняют Firecrawl (конкретные URL), Apify (скрап), x_search (соцсети). Использовать как вспомогательную информацию и образцы — **НЕ копирование**.

### Этап 3 — Подключение и роль Firecrawl (21.06.2026)
Firecrawl подтверждён эмпирически (прямой запрос к `api.firecrawl.dev`, ключ `fc-8105…` валиден) и встроен
в Правило 5 `SOUL.md` как **«парсер конкретного URL/сайта»** — пара Perplexity по разведке: Perplexity отвечает
на вопрос «что происходит в нише», Firecrawl — «дай мне реальный контент вот этого сайта/страницы».

Проверены 4 режима (см. `Hermes-config/Firecrawl-setup.md` для деталей и примеров):
- **`scrape`** — страница → чистый markdown (1 кредит)
- **`map`** — сайт → список всех его URL (0 кредитов, бесплатно)
- **`search`** — лёгкий веб-поиск без синтеза, просто ссылки+сниппеты (≈1.3 кредита/результат)
- **`v2/scrape` с форматом `json`** — страница → структурированные данные по схеме (5 кредитов); заменяет
  устаревший `/v1/extract` (тот же результат, но 22 кредита — **не использовать**)

В пайплайне разведки конкурентов (Foresight / `market-competitor-analyst`) роль Firecrawl: `map` сайта
конкурента → выбрать релевантные страницы (цены/фичи/блог) → `v2/scrape json` по каждой с нужной схемой
(цена, тарифы, фичи) → готовая таблица для сравнения. Дешевле и точнее, чем просить LLM пересказать сайт
по памяти.

### Этап 4 — OpenRouter (расширенный роутинг моделей) (21.06.2026)
OpenRouter подтверждён эмпирически (`GET /api/v1/auth/key` → ключ валиден; реальный `chat_completion` на
`anthropic/claude-haiku-4.5` → HTTP 200, `usage.cost = $0.000034`) и переписан в Правило 4 `SOUL.md`
с нуля — обнаружен и исправлен реальный баг (id моделей были с дефисом вместо точки, что вернуло бы
404/400 при первом реальном вызове).

Два независимых механизма, не сводящихся к одному вызову модели:
- **`chat_completion` (MCP)** — разовый вызов другой модели под конкретную подзадачу (vision, гигантский
  контекст, формальная логика, дешёвый быстрый ответ) — таблица в `SOUL.md` §4.1.
- **Pareto Code Router** (`openrouter/pareto-code` + `min_coding_score: 0.65`) — авто-выбор самой дешёвой
  модели, проходящей порог качества кода. Подтверждено в исходнике, что порог model-gated — работает
  только на этой конкретной модели.
- **Response cache** (`response_cache`/`response_cache_ttl`) — повтор идентичного запроса в течение 300с
  бесплатен (заголовки `X-OpenRouter-Cache`).
- **Fallback-провайдер** — поддерживается (~30 провайдеров), но `fallback_providers: []` пока пуст,
  осознанно не активирован (поведенческое изменение для прода).

DeepSeek v4 (`deepseek-chat`) остаётся основной моделью — OpenRouter не заменяет её, а дополняет точечными
вызовами. Детали и примеры — `Hermes-config/OpenRouter-setup.md`.

**Добавление (22.06.2026, версия 1.5.0) — MiniMax M3:** найдена и подключена модель `minimax/minimax-m3`
через тот же канал OpenRouter (без новых секретов). Контекст 1 048 576 токенов, цена $0.30/$1.20 за 1M —
дешевле всех моделей в таблице роутинга. Проверена реальной задачей (логическая загадка + код на Python —
оба решения верны), не просто пингом. Найдена и задокументирована особенность: один из backend-провайдеров
OpenRouter (`AtlasCloud`) в 1 из 4 тестов продублировал ответ целиком; решение (`provider_routing.ignore`)
готово, но не активировано — глобальная настройка, трогаем только по явному запросу. Позиционирована в
`SOUL.md` §4.1/§4.5/§9 как модель, которую пробуем ПЕРВОЙ для длинных документов и reasoning/код-задач.

### Этап 5 — Notion как база знаний находок (22.06.2026, версия 1.6.0)
Notion был подключён как MCP ещё в версии 0.0.5 (15.06.2026), но до этого этапа не имел роли в
оркестрации — ключ работал, но ни одного правила «когда использовать» не было. Теперь встроен как
**третий, отдельный слой** рядом с роутингом инструментов (§5) и учётом расходов (§8):

- **Архитектура (эмпирически создана 22.06.2026):** корневая страница `Foresight — База знаний (Hermes)`
  → база данных `Исследования и находки` (свойства `Name`/`Дата`/`Инструмент`/`Категория`/`Стоимость ($)`/
  `Версия проекта`). Не заменяет `cost-ledger.md` (финансы) и не заменяет `SOUL.md` (правила) — это архив
  **содержательных находок**, не каждого мелкого запроса.
- **Связь с уже подключённым пайплайном:** после значимого результата от Perplexity/Firecrawl/OpenRouter
  (рыночные данные, особенность модели/провайдера) — сохранять находку в Notion. Перед новым платным
  исследованием — сначала `search` в этой базе (правило экономии §9): дешёвый Notion-поиск вместо
  повторного платного запроса.
- **Первая запись в базе** — находка AtlasCloud/MiniMax M3 (см. Этап 4 выше, §4.5 `SOUL.md`) — перенесена
  из текста SOUL.md в Notion как пример «как выглядит находка в базе».
- Найден и исправлен баг кодировки: создание страниц с кириллицей через `curl`+heredoc давало битый
  UTF-8 — исправлено переходом на Python `urllib.request` с явным `ensure_ascii=False`. Корректность
  проверена побайтово (hex заголовка), а не на глаз — терминал искажал кириллицу чисто визуально.
- Подробности и проверка — `Hermes-config/Notion-setup.md`; правило — `SOUL.md` §10 (+ строка в §5).

### Этап 6 — PostMyPost как слой публикации (22.06.2026, версия 1.7.0)
PostMyPost подключён как MCP ещё в версии 0.3.x, но не имел роли в оркестрации. Встроен как
**слой публикации/дистрибуции** — противоположность Perplexity/Firecrawl (те добывают информацию,
PostMyPost раздаёт готовый контент в соцсети) и пара к Notion (лог факта публикации):

- **Проверка эмпирически:** MCP без сессий (Remote HTTP, JSON-RPC); подтверждены все 15 инструментов;
  найден существующий проект «Личный проект» (`id 347765`); каталог 23 платформ через `list_channels`.
- **⚠️ Найден блокер:** `list_accounts`/`list_rubrics` возвращают «Срок действия вашего тарифа истёк» —
  реальная публикация (`create_publication`) сейчас не сработает, нужно продление тарифа пользователем.
  Подключение/токен/MCP — рабочие, заблокирована именно платная функция самого PostMyPost.
- **Архитектура (готова, активируется после продления тарифа):** текст готов (Hermes/DeepSeek или
  департамент `copywriter`/`content-lead`) → выбрать аккаунт/канал (`list_accounts`/`list_rubrics`) →
  медиа при необходимости (`init_upload`/`complete_upload`) → **обязательное подтверждение пользователя**
  перед `create_publication` (публикация в соцсеть видна другим, трудно обратима) → лог в новую Notion-базу
  **«Публикации (PostMyPost)»** (дата/канал/статус/ссылка) → позже `get_analytics_publications` для метрик.
- Создана и проверена побайтово вторая Notion-база (отдельная от «Исследования и находки» — другая
  природа данных). Пока без записей — появятся при первой реальной публикации.
- Подробности и проверка — `Hermes-config/PostMyPost-setup.md`; правило — `SOUL.md` §11.

### Этап 7 — YouGile как учёт задач (22.06.2026, версия 1.8.0)
YouGile подключён как MCP с 16.06.2026, но не имел роли в оркестрации. Встроен как **четвёртый слой** —
активные to-do со статусом/дедлайном, в отличие от Notion (архив прошлого, §10) и PostMyPost (выход
вовне, §11):

- **Проверка эмпирически:** в компании уже было демо-содержимое YouGile («Example Project» с
  доской-шаблоном на русском) — не трогали. Создана отдельная структура: проект
  `Foresight — Задачи (Hermes)` → доска `Задачи Hermes` → 3 колонки (`Входящие`/`В работе`/`Готово`).
- **Конкретная связь сервисов:** первая реальная задача — «Продлить тариф PostMyPost» (колонка
  `Входящие`) — напрямую связывает находку из Этапа 6 (блокер тарифа PostMyPost) с YouGile как местом,
  где это действие не потеряется в переписке. Когда тариф продлён — задача закрывается.
- **Когда создавать:** найден блокер/действие на будущее (не закрывается одним ответом сейчас),
  многошаговая работа, или по явному запросу «поставь задачу».
- Заголовки проверены побайтово (UTF-8 hex) — совпали с первого раза.
- Подробности и проверка — `Hermes-config/YouGile-setup.md`; правило — `SOUL.md` §12.

### Этап 8 — Vercel как слой статуса деплоя (22.06.2026, версия 1.9.0)
Vercel подключён как community MCP (`@open-mcp/vercel`) ранее, но не имел роли в оркестрации. Встроен
как **слой статуса деплоя/наблюдаемости** — в отличие от PostMyPost (выход/публикация с подтверждением)
Vercel в первую очередь читает состояние и докладывает, запись — отдельная, осторожная операция:

- **Проверка эмпирически:** прямые REST-вызовы `https://api.vercel.com` (`GET /v2/user`, `/v9/projects`,
  `/v6/deployments`) — все HTTP 200. Аккаунт на плане hobby (бесплатный). Найден существующий проект
  `test-landing-vercel-project-core` и его реальная история деплоев (последний `READY`, коммит
  «traffictime | version 1.5.0»).
- **⚠️ Жёсткое ограничение:** никаких изменений прод-окружения без явной команды пользователя — push
  в GitHub не деплоит автоматически, новый деплой по умолчанию preview, alias главного домена менять
  только по прямой команде. Правило перенесено из отдельного проекта лендинга CORE и закреплено как
  общее правило Hermes для любого Vercel-проекта.
- **Конкретная связь сервисов:** если в логах деплоя находится значимая техническая находка — заносить
  в Notion (§10); если деплой падает и требует ручного вмешательства — заводить задачу в YouGile (§12),
  по аналогии с задачей о тарифе PostMyPost.
- Только чтение выполнялось — без write-вызовов (новый деплой/смена alias/удаление).
- Подробности и проверка — `Hermes-config/Vercel-setup.md`; правило — `SOUL.md` §13.

### Этап 9 — Smithery как реестр-разведчик новых MCP-серверов (22.06.2026, версия 1.10.0)
Smithery подключён через OAuth2+PKCE bridge ещё с 07.06.2026, но не имел роли в оркестрации. Встроен
как **точка обнаружения** — единственный сервис из встроенных, который не решает конкретную задачу,
а отвечает на вопрос «есть ли уже готовый MCP-сервер под новую потребность»:

- **Проверка эмпирически:** namespace `vfvf6462` — 0 собственных серверов (пользователь/Hermes —
  потребители реестра). Найдено 2 подключения: рабочее `smithery-registry` (`connected`, 46
  инструментов) и незавершённый черновик `smithery` (`input_required`, не трогаем).
- **Конкретная связь сервисов (демонстрация):** `servers_list(q="sentry")` нашёл официальный
  verified-сервер `qualifiedName: "sentry"` (`bySmithery: true`, remote, `sentry.run.tools`) — Sentry
  был в TODO-списке неподключённых сервисов. Находка записана в Notion («Исследования и находки»,
  категория «Техническая находка»), задача «Подключить Sentry MCP (найден через Smithery)» создана
  в YouGile (колонка «Входящие») — оба заголовка проверены побайтово (UTF-8 hex).
- **Когда использовать:** перед ручным поиском интеграции под новую возможность — сначала
  `servers_list`/`servers_get` в Smithery.
- ⚠️ Не путать с «Skill SH» (`npx skills find`) — отдельный реестр скиллов, не связанный со Smithery.
- Подробности и проверка — `Hermes-config/Smithery-setup.md`; правило — `SOUL.md` §14.

### Этап 10 — Replicate как слой генерации медиа (22.06.2026, версия 1.11.0)
Replicate подключён в `config.yaml` ранее, но не имел роли в оркестрации — хотя уже неформально
использовался департаментом CORE (Replicate MCP назван в стеке `visual-image-artist`, `audio-producer`,
`threed-artist`). Встроен как **формализация уже распределённой практики**, а не новая возможность:

- **Проверка эмпирически:** `GET /v1/account` подтвердил валидный токен (организация `vanquish`).
  Найден баг прямого REST-доступа — Cloudflare блокировал запросы (HTTP 403, `error code: 1010`) из-за
  дефолтного User-Agent `urllib`, не из-за токена; исправлено браузерным заголовком.
- **Реальный тест полного цикла:** генерация изображения моделью `flux-schnell` (`Prefer: wait`) —
  `succeeded` за 0.54 сек, получена ссылка на результат. Найдена 1 предсуществующая генерация
  (`flux-1.1-pro`, 15.06.2026) — не созданная в рамках этого подключения.
- **Конкретная связь сервисов:** Replicate MCP уже в стеке трёх специалистов CORE-департамента
  (изображения/аудио/3D-текстуры) — это подключение закрепляет правило для них на уровне Hermes;
  сам Hermes использует Replicate напрямую, когда пользователь в Telegram просит генерацию, а Gemini
  не подходит.
- **Отличие в учёте расходов:** в отличие от большинства уже подключённых сервисов (фиксированный план),
  Replicate тарифицируется за вызов генерации — каждая реальная генерация это строка в `cost-ledger.md`.
- Подробности и проверка — `Hermes-config/Replicate-setup.md`; правило — `SOUL.md` §15.

### Этап 11 — ElevenLabs как слой озвучки/голоса (22.06.2026, версия 1.12.0)
ElevenLabs подключён в `config.yaml` ранее (и как MCP, и как built-in провайдер `tts`/`stt`), но не
имел роли в оркестрации. Уже неформально использовался департаментом CORE (ElevenLabs MCP назван в
стеке `audio-producer`, в той же строке, что Replicate). Встроен как **формализация существующей
практики + явное разграничение трёх слоёв одного сервиса**:

- **Проверка эмпирически:** ключ валиден, но ограничен по правам (нет `user_read` →
  `/v1/user`/`/v1/user/subscription` вернули 401, при этом `/v2/voices`/`/v1/models`/`/v1/convai/agents`
  штатно 200) — не сломанный ключ, баланс/план через API не прочитать.
- **Реальный тест полного цикла:** синтез речи (`eleven_multilingual_v2`, голос Adam, 26 символов) —
  получен настоящий mp3 (28 465 байт), подтверждено записью в `/v1/history`. `/v1/convai/agents` —
  0 предсуществующих агентов, чистый лист.
- **Три слоя одного сервиса:** (1) built-in `tts.provider`/`stt.provider` в `config.yaml` — сейчас
  активны бесплатные `edge`/`local`, ElevenLabs премиум-альтернатива, не включена по умолчанию;
  (2) MCP-инструмент для прямых запросов (конкретный голос/клонирование/диаризация/Conversational AI);
  (3) департамент CORE (`audio-producer`) — уже использует ElevenLabs (голос) рядом с Replicate
  (музыка, §15) в одном специалисте.
- **Отличие в учёте расходов:** как и Replicate, тарификация за вызов (по символам), не фиксированным
  планом.
- Подробности и проверка — `Hermes-config/ElevenLabs-setup.md`; правило — `SOUL.md` §16.

### Этап 12 — Sentry как слой мониторинга ошибок (22.06.2026, версия 1.13.0)
Sentry подключён в `config.yaml` ранее (официальный MCP, OAuth2 через `mcp-remote`, без API-ключа),
но не имел роли в оркестрации — был найден через **Smithery** (`servers_list(q="sentry")`, Этап 9) и
ждал подключения в задаче YouGile (Этап 7). Этот этап — первый, где цикл реально **закрыт**, а не
просто описан:

- **Проверка эмпирически:** OAuth уже был пройден ранее — найдена и использована техническая
  особенность `mcp-remote`: токены кэшируются в `~/.mcp-auth/mcp-remote-<версия>/<MD5(url сервера)>_tokens.json`,
  имя файла — это MD5-хеш URL MCP-сервера, что позволило переиспользовать готовый токен напрямую,
  без браузерного логина. Также найден тот же баг Cloudflare 403, что у Replicate (Этап 10) —
  исправлен тем же браузерным User-Agent.
- **Реальная проверка инструментов:** `tools/list` подтвердил 8 инструментов (`find_organizations`,
  `find_projects`, `search_events`, `search_issues`, `analyze_issue_with_seer`, `get_sentry_resource`,
  `search_sentry_tools`, `execute_sentry_tool`). `find_organizations` → организация `vanquish-project`;
  `find_projects` → 0 проектов — чистый аккаунт, SDK ещё не интегрирован ни в один реальный проект
  (отдельный будущий шаг, не входит в это подключение).
- **Закрытие цикла, а не только описание:** задача YouGile «Подключить Sentry MCP (найден через
  Smithery)» реально перенесена в колонку «Готово» и помечена `completed: true`; находка о состоянии
  аккаунта и об открытии с кешем OAuth-токенов реально записана в базу Notion «Исследования и находки».
- Подробности и проверка — `Hermes-config/Sentry-setup.md`; правило — `SOUL.md` §17.

### Этап 13 — LangSmith как слой трассировки LLM-вызовов (23.06.2026, версия 1.14.0)
LangSmith подключён в `config.yaml` ранее (`langsmith-mcp-server` через `uvx`), но не имел роли в
оркестрации. Этот этап даёт ему роль: **слой трассировки и диагностики содержимого LLM-вызовов** —
пошаговый разбор «промпт → вызовы инструментов → ответ модели», в отличие от Helicone (сколько
потрачено) и Sentry (что упало в коде):

- **Проверка эмпирически:** REST API (`https://api.smith.langchain.com`) отвечает, ключ валиден
  (`/info` → версия `0.16.6`). Cloudflare-бага User-Agent (как у Replicate/Sentry, Этапы 10/12) не
  возникло — проверяли защитно с браузерным заголовком с первого запроса.
- **Состояние аккаунта:** один предсуществующий проект «foresight» (создан 08.06.2026, раньше начала
  этой оркестрации) — **0 трассированных вызовов**, **0 датасетов**, аккаунт на **бесплатном тарифе**
  (org «Personal», лимит расходов на движок $0).
- **Ключевая находка:** MCP-сервер и ключ работают, но Hermes пока не отправляет туда трейсы своих
  реальных вызовов — нужна отдельная интеграция в код (не входит в это подключение). Та же логика
  честности, что у Sentry (Этап 12): подключение MCP и реальная интеграция в живой код — два разных
  шага, сделан только первый.
- **Роль на этом этапе:** задел на будущее (датасеты/eval) + ручной инструмент диагностики конкретного
  инцидента (например, если повторится дублирование ответа у AtlasCloud, §4.5) — не постоянный
  автоматический слой, как уже является Helicone для затрат.
- Подробности и проверка — `Hermes-config/LangSmith-setup.md`; правило — `SOUL.md` §18.

---

## Статус
- Подход **рабочий**: его ядро (Правило 5) уже живёт в `SOUL.md` и применяется Hermes.
- Инструменты, подключённые и встроенные в роутинг: **Perplexity** (вопрос→ответ с источниками),
  **Firecrawl** (URL/сайт→контент или структура), **OpenRouter** (вызов другой модели под подзадачу +
  Pareto Code Router + response cache), **Notion** (база знаний значимых находок — третий слой рядом
  с роутингом инструментов и учётом расходов), **PostMyPost** (слой публикации/дистрибуции контента,
  заблокирован истёкшим тарифом — см. Этап 6), **YouGile** (учёт активных задач — четвёртый слой,
  см. Этап 7), **Vercel** (статус деплоя/наблюдаемость, только чтение по умолчанию — см. Этап 8),
  **Smithery** (реестр-разведчик новых MCP-серверов — см. Этап 9), **Replicate** (слой генерации
  медиа — изображения/аудио/3D-текстуры, формализует практику CORE-департамента — см. Этап 10),
  **ElevenLabs** (озвучка/голос — три слоя: built-in TTS/STT провайдер, MCP, CORE-департамент —
  см. Этап 11), **Sentry** (мониторинг ошибок и AI-диагностика, цикл «найден через Smithery → задача
  в YouGile → подключён и закрыт» — см. Этап 12), **LangSmith** (трассировка и диагностика содержимого
  LLM-вызовов, пока без автоматической интеграции — задел на будущее + ручной инструмент при
  конкретных инцидентах — см. Этап 13).
- Дальнейшее расширение — по тому же принципу: добавлять категории в матрицу инструментов
  (изображения / видео / аудио / 3D) — см. общий TODO Tool Routing Algorithm.
- Альтернатива (департамент) — «Инструкция к оркестрации 2 (Мой вариант)», на паузе до активации.
